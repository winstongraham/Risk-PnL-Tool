def calculate_pnl(): pass

def calculate_pnl(prices, entry_prices, quantites):
      """
    prices: dict of latest market prices (e.g., {'AAPL': 160.0})
    entry_prices: dict of entry prices for each instrument (e.g., {'AAPL': 150.0})
    quantities: dict of quantities (e.g., {'AAPL': 1000})
    
    Returns: dict of unrealized PnL per instrument
    """
def calculate_portfolio_value(prices, quantities):
    """
    Returns the total value of the portfolio at current prices.
    """

def calculate_daily_returns(price_series):
    """
    price_series: pandas DataFrame with historical prices (rows = dates, cols = symbols)
    
    Returns: DataFrame of daily % returns
    """
